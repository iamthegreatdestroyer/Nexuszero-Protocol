# Template for Kubernetes Secrets
# DO NOT commit actual secrets to version control
# Use kubectl create secret or external secret management tools

# This file is provided as a template only and should NOT be applied as-is
# All secrets must be created using secure methods:

# Method 1: Create from command line
# kubectl create secret generic nexuszero-secrets \
#   --from-literal=GRAFANA_ADMIN_PASSWORD='your-secure-random-password' \
#   --namespace=nexuszero

# Method 2: Use external secrets manager
# - HashiCorp Vault
# - AWS Secrets Manager
# - Azure Key Vault
# - Google Secret Manager

# Example secret structure (DO NOT USE THESE VALUES):
# apiVersion: v1
# kind: Secret
# metadata:
#   name: nexuszero-secrets
#   namespace: nexuszero
# type: Opaque
# stringData:
#   # Database credentials
#   DB_PASSWORD: "CHANGE_ME_TO_SECURE_PASSWORD"
#   
#   # API keys
#   API_KEY: "CHANGE_ME_TO_SECURE_KEY"
#   
#   # JWT secret (use openssl rand -base64 32)
#   JWT_SECRET: "CHANGE_ME_TO_SECURE_JWT_SECRET"
#   
#   # GitHub token
#   GITHUB_TOKEN: "CHANGE_ME_TO_GITHUB_PAT"
#   
#   # Grafana admin password (use openssl rand -base64 16)
#   GRAFANA_ADMIN_PASSWORD: "CHANGE_ME_TO_SECURE_PASSWORD"

# To generate secure random passwords:
# openssl rand -base64 16  # For passwords
# openssl rand -base64 32  # For JWT secrets and API keys

---
# Example: Create secrets from command line instead:
# kubectl create secret generic nexuszero-secrets \
#   --from-literal=DB_PASSWORD='your-password' \
#   --from-literal=API_KEY='your-api-key' \
#   --namespace=nexuszero

# Example: Create secrets from file:
# kubectl create secret generic nexuszero-secrets \
#   --from-file=.env \
#   --namespace=nexuszero

# Example: Use external secrets operator with AWS Secrets Manager, HashiCorp Vault, etc.
